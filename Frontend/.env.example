# API Configuration
# Using proxy in development, so default to /api
VITE_API_URL=/api

# For direct connection without proxy:
# VITE_API_URL=http://localhost:3000/api

# Environment
VITE_ENV=development
